import 'package:flutter/material.dart';

class RocketsListPage extends StatelessWidget {
  const RocketsListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}